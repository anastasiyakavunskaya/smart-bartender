<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.user.SmartBartender.SettingsFragments.IngredientsFragment">

    <android.support.v7.widget.RecyclerView
        android:id="@+id/ing_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        />
    <TextView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:padding="@dimen/padding"
        android:textSize="@dimen/item_text_size"
        android:id="@+id/ingredients_warning_text"/>
<LinearLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="bottom"
    android:orientation="horizontal">

    <EditText
        android:id="@+id/edit_ingredient"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="3"
        android:background="@android:color/white"
        android:hint="@string/ingredient_input_text"
        android:inputType="textShortMessage|text"
        android:textColor="@color/colorPrimaryDark"
        tools:ignore="Autofill"
        android:padding="@dimen/padding"/>

    <Button
        android:id="@+id/save_btn"
        android:background="@color/colorPrimaryDark"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:textColor="@android:color/white"
        android:text="@string/save_button"
        android:layout_weight="1"/>
</LinearLayout>
    </FrameLayout>
